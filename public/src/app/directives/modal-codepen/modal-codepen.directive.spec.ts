import { ModalCodepenDirective } from './modal-codepen.directive';

describe('ModalCodepenDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalCodepenDirective();
    expect(directive).toBeTruthy();
  });
});
